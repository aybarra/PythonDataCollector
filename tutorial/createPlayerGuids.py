# Need to execute the API call to create a guid for each of the player names
        # Ex: http://127.0.0.1:8000/pfrguids/ pro_football_ref_name="BreeDr00" player_full_name="Drew Brees"
        # auth=HTTPBasicAuth('user', 'pass'))
#        r = requests.post("http://127.0.0.1:8000/pfrguids/", data = {"pro_football_ref_name": pfr_name, "player_full_name": player_name},
#                          auth=HTTPBasicAuth('andrasta', 'aA187759!'))
